# Deployment

Canonical guide: **[docs/DEPLOYMENT.md](./docs/DEPLOYMENT.md)**

Quick path when credentials arrive: fill `apps/web/.env.production.example` → Vercel env → domain → Deploy → smoke `/api/health` + `/api/ready`.
