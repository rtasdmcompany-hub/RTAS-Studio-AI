RTAS Studio — preview video

Replace this file with your own cinematic sample:

  preview-showcase.mp4

The Preview panel plays this local file (no animated demo, no buffering).

Recommended: MP4, 1920x1080 or higher, 10–30 seconds, H.264.

To re-download a default sample:
  npm run fetch:preview -- --force
