# RTAS Backend

FastAPI GPU generation API (port 8000). Python-only — started via the workspace script or `@rtas/backend` npm wrapper.

```bash
npm run dev:backend
```

See `requirements.txt` for Python dependencies and `main.py` for route registration.
