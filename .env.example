# =============================================================================
# RTAS Studio AI — monorepo root env pointer
# =============================================================================
# Application secrets live in workspace templates:
#
#   apps/web/.env.example              → local Next.js (copy to .env.local)
#   apps/web/.env.production.example   → Vercel / production checklist
#   apps/backend/.env.example          → FastAPI GPU worker
#
# Full documentation: docs/ENVIRONMENT.md
# NEVER put production secrets in this file or commit .env.local.
# =============================================================================

# Optional: set when running root scripts that need a default workspace hint
# RTAS_WEB_ENV_FILE=apps/web/.env.local
